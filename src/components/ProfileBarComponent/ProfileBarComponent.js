import React from 'react';
import PropTypes from 'prop-types';
import styles from './ProfileBarComponent.module.scss';

const ProfileBarComponent = () => (
  <div className={styles.ProfileBarComponent}>
    ProfileBarComponent Component
  </div>
);

ProfileBarComponent.propTypes = {};

ProfileBarComponent.defaultProps = {};

export default ProfileBarComponent;
